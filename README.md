```markdown
## cách tải folder lên github
git init 
git remote add origin https:...

git add ./
git commit -m"abc"
git branch -M codepython
git pull origin codepython --rebase
git push -u origin codepython

#hướng dẫn cài đặt và chạy dự án
1. clone repo về máy
   git clone https://github.com/AdleyPanas/DATN.git
